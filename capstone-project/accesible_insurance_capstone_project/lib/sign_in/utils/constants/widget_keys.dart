import 'package:flutter/material.dart';

class SigninWidgetKeys {
  static const screenKey = ValueKey('screen');
  static const passwordInputKey = ValueKey('loginInput');
  static const emailInputKey = ValueKey('emailInput');
  static const signinButtonKey = ValueKey('signin');
}
